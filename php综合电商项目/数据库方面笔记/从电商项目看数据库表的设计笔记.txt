
    1， 主键的大致 设计 基本相同  id mediumint(或者int) unsigned not null auto_increment comment 'Id',
 
   2, 除了类型是文本字符串的  text 或者 longtext ,其他的 字段我们都 必须设置  not null  
   
   3， Text 类型不能有默认值 。
   
   4，  设置字段 not null ,那么我们 必须问自己，是否 用户可以不填， 如果 用户 可以不填写 ，那么 我们可以 给默认值
      
	             一般的默认值 就是   int    0
				 
				                     varchar ''
									 
    5, 设置 为 唯一键 的字段  
	       第一，设置可以 为 null ，null 和 null 是不相等的，所有没事
		   
		   第二，如果设置  not null ，那么不能设置默认值
		   
	6，对于那些 not null 而且 没有设置 默认值的 字段 ，我们必须 在 模型里面 自动验证 里面去验证 ，因为  空字符串 ''  是可以插入

              成功的 ，not null 仅仅是限制 不能插入 null ,  而且 必须 要设置  规则是 一定要 验证，第4个是 1，不设置为 1，那么默认就是 0，
			  
			  也就是 没有获取这个 字段就不验证， 那么 在前台  人为 的去掉 这个字段的 name，或者 去掉这个 input，那么最终 结果还是 以 
			  
			  空字符串  插入 成功，这样子也不是我们要的结果。所以 ，必须设置 为 1.

             array('goods_name', 'require', '商品名称不能为空！', 1),	
   
    7，对于 输入 表单的数据 ，我们必须 为所以的表单设置一个 过滤函数 ，本身默认是  htmlspecialchars ，我们在给加上一个 trim 函数。这个函数会在
	
	  I() 函数 帮我们获取 数据的时候 自动调用 。
	
	   'DEFAULT_FILTER' => 'trim,htmlspecialchars',
	   
	8，对于 插入 数据库的 时候 ，我们 必须设置 可以接受的 字段 ，比如 插入的时候 ，前端 随便给了个 id   99999999

           如果此时我们接受了这个字段，并且 插入到数据库中了，那么我们 后面 就在也 插入不了 数据了 。所以
		   
		    // 添加时调用create方法允许接收的字段
	protected $insertFields = 'goods_name,market_price,shop_price,is_on_sale,goods_desc';
	
	9， 一般的 字段 我们必须用   I()   处理，但是 如果 用的是 富文本编辑器 ，那么我们不能这么全部处理，要不然的，先处理之后，我们在显示的时候 
	
	还要 处理过来 ，不像别的字段 处理过后就不处理过来显示到前台，那么如果 富文本 里面有 恶意的 js 代码，那么会给我们的程序 带来危害，那么 
	
	我们用的是  htmlpurifier  ，处理的是 原始接收来的 $_post[ ]  字符串

   

     10 ， 设置字段not null ，没有 设置 字段的 的默认值 ，也没有 传递 这个字段的 值 ，那么 在插入的时候 字段就会自己以  默认值来插入
	 
	     默认值 就是    int  -> 0     char ->'' 空字符串    datetime -> 0000-00-00 00:00:00  enum('男','女',) 默认第一个  男 
		 
		              decimal(10,2)-> 0.00   date ->0000-00-00   set('足球，排球') ->'' 空字符串 

                    系统 都会用 这个 作为 默认值 来 最终插入 。
					
					
		  text 我们不能设置默认值  ，但是 在实践中 发现 ，如果 null ，那么默认就是 null ,如果 not null ，那么默认值就是 '' 空字符串			
					
					
					
					
					
					
					
					
					
					
					
					
					
					
					












		   